document.querySelector("#ssh-validate-button").addEventListener("click", () => {


	console.log("SSH验证按钮被点击了");

	    // localStorage.setItem("20250528T0947270800", "true");


//   const host = this.container.querySelector("#host-input").value.trim();
//   const port = parseInt(this.container.querySelector("#port-input").value.trim());
//   const user = this.container.querySelector("#user-input").value.trim();
//   const pubkey = this.container.querySelector("#pubkey-input").value.trim();

//   const errors = [];

//   // 格式校验略...

//   if (errors.length === 0) {
//     // 假设这里验证成功
//     resultMessage.textContent = "✅ 验证通过，正在加载内容...";
//     resultMessage.style.color = "green";

//     // 写入 localStorage
//     localStorage.setItem("20250528T0947270800", "true");

//     // 切换显示区域
//     document.getElementById("validator-section").style.display = "none";
//     document.getElementById("protected-content").style.display = "block";
//   } else {
//     resultMessage.innerHTML = "❌ 验证失败：<br />- " + errors.join("<br />- ");
//     resultMessage.style.color = "red";
//   }
});